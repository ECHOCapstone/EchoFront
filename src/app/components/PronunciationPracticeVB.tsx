// V/B 발음 연습 라우트는 PronunciationPractice 동적 컴포넌트로 위임된다.
import PronunciationPractice from './PronunciationPractice';

export default function PronunciationPracticeVB() {
  return <PronunciationPractice />;
}
